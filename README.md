# Solar Panel Cleanliness Classifier

This is a Streamlit web app that classifies solar panel images as Clean or Dirty using a trained CNN model.

## How to run:

```bash
pip install -r requirements.txt
streamlit run app.py
